/**
 * contains a list of all user groups in the system
 */
export const RoleBasedPermissionGroups = Object.freeze({
    AppAdmin: "AppAdmin",
    Admin: "Admin",
    User: "User",
    AuthBase: "AuthBase",
});
